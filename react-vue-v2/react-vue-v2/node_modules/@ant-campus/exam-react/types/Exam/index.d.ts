import { FC, ReactNode } from 'react';
import 'github-markdown-css';
export interface IExamProps {
    title: string;
    description: string;
    render?: () => ReactNode;
    test?: () => boolean | Promise<boolean>;
}
export declare const Exam: FC<IExamProps>;
