import { FC, PropsWithChildren, ReactNode } from 'react';
import 'github-markdown-css';
import './style.less';
export interface IMarkdownProps {
    text: string;
    before?: ReactNode;
    after?: ReactNode;
}
export declare const Markdown: FC<PropsWithChildren<IMarkdownProps>>;
