import { FC, ReactElement } from 'react';
import { Exam, IExamProps } from '../Exam';
export interface IExamDef extends IExamProps {
}
export interface IPaperProps {
    exams: Array<IExamDef | ReactElement<IExamProps, typeof Exam>>;
}
export declare const Paper: FC<IPaperProps>;
